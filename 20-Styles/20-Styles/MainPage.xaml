<Page
    x:Class="_20_Styles.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:_20_Styles"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <Style TargetType="Button" x:Key="BigFontButtonStyle">
            <Setter Property="Control.FontFamily" Value="Times New Roman" />
            <Setter Property="Control.FontSize" Value="18" />
            <Setter Property="Control.FontWeight" Value="Bold" />
            <Setter Property="Control.Background" Value="BlueViolet" ></Setter>
            <Setter Property="Control.Foreground" Value="White"></Setter>
            <!--<Setter Property="Control.Padding" Value="10" ></Setter>-->
            <Setter Property="Control.Margin" Value="20"></Setter>
           
        </Style>
        <Style x:Key="boton2"  TargetType="Button">
            <Setter Property="Control.Foreground" Value="Yellow"></Setter>
        </Style>
    </Page.Resources>

    <RelativePanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel >
            <Button Content="Boton 1" Style="{StaticResource BigFontButtonStyle}"></Button>
            <Button Content="Boton 1" Style="{StaticResource boton2}"></Button>
        </StackPanel>

    </RelativePanel>
</Page>
